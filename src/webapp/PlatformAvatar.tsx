import React from "react";
import { Avatar } from "@material-ui/core";
import { Computer } from "@material-ui/icons";

export default function PlatformAvatar(props: { abbrev: string }): JSX.Element | null {
    return <Computer />;
}
